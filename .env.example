# Server Configuration
PORT=3000
NODE_ENV=development

# Supabase Configuration (Database + Storage)
# Sign up at: https://supabase.com
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_KEY=your_supabase_anon_key_here


# Pinecone Configuration (Vector Database for RAG) - 1024 dimensions
# Sign up at: https://www.pinecone.io
PINECONE_API_KEY=your_pinecone_api_key_here
PINECONE_INDEX_NAME=chatbot
PINECONE_ENVIRONMENT=us-east-1
PINECONE_HOST=https://your-index.svc.pinecone.io

# OpenAI Configuration (Chat + Embeddings)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here



# Chat model options:
# gpt-4o-mini (recommended, fast & economical, great quality)
# gpt-4o (best quality, more expensive)
# gpt-4-turbo (excellent, good for complex reasoning)
# gpt-4 (high quality, slower)
OPENAI_MODEL=gpt-4o-mini

# Embeddings - OpenAI text-embedding-3-large (1024 dims, superior quality)
EMBEDDING_MODEL=text-embedding-3-large
EMBEDDING_DIMENSIONS=1024

# Cohere Reranking (optional but recommended for better RAG results)
# Sign up at: https://cohere.com
COHERE_API_KEY=your_cohere_api_key_here


# Manager emails (comma-separated for multiple recipients)
# Daily summaries will be sent to all addresses listed
MANAGER_EMAIL=manager@yourdomain.com,admin@yourdomain.com


# Timezone for daily summary
TIMEZONE=America/Chicago
SUMMARY_TIME=06:00

# Cron Secret (for securing cron endpoints)
# Generate with: openssl rand -base64 32
CRON_SECRET=your_cron_secret_here

# Admin Password Hash (bcrypt)
# Generate with: npx tsx -e "import bcrypt from 'bcrypt'; bcrypt.hash('your-password', 10).then(console.log)"
# Or use online tool: https://bcrypt-generator.com (10 rounds)
ADMIN_PASSWORD_HASH=$2b$10$YourBcryptHashHere

# Vercel KV (Redis) Configuration
# Create KV database in Vercel dashboard: https://vercel.com/dashboard/stores
# These environment variables are automatically set by Vercel when you create a KV store
KV_REST_API_URL=https://your-kv-url.upstash.io
KV_REST_API_TOKEN=your_kv_token_here

# Resend Email Configuration
# Sign up at: https://resend.com
RESEND_API_KEY=your_resend_api_key_here
RESEND_FROM_EMAIL=noreply@yourdomain.com

# CORS Configuration for Embed Widget
# Comma-separated list of allowed origins for embedding the chatbot
# Use '*' to allow all origins (not recommended for production)
# Use '*.yourdomain.com' for wildcard subdomains
# Example: https://example.com,https://www.example.com,*.partner-site.com
ALLOWED_ORIGINS=*

# API Key Validation for Embed Widget (optional)
# Set to 'true' to require API keys for external website embeds
# Generate API keys with: openssl rand -hex 32
REQUIRE_API_KEY=false
VALID_API_KEYS=your_api_key_1,your_api_key_2
